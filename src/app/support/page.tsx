import { InfoPage } from "@/components/InfoPage";

export default function SupportPage() {
  return (
    <InfoPage eyebrow="MemberAccessRecon" title="Support">
      <p>Email support@konverter-pro.de for installation help, report questions, deletion requests, and billing issues.</p>
      <p>
        For review, include your agency ID, affected location IDs, and whether the issue involves subscription status,
        payment status, course access, CSV roster import, CSV export, or PDF export.
      </p>
      <p>Normal response target: within two business days.</p>
    </InfoPage>
  );
}
